<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed ? "[X]" : "[ ]"%></p>

<%= link_to "Edit task", edit_path %>

<%= link_to "Go back to menu", index_path %>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
